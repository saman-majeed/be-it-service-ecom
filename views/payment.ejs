<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/head') %>
        <title>Be IT: Payment</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/css/checkout.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/">
                <span class="d-inline-flex align-items-center">
                    <span class="bubble">Be</span> IT:SERVICE
                </span>
            </a>
        </div>
    </nav>

    <div class="container checkout-box" style="max-width: 500px;">
        <h3 class="section-title text-center">Secure Payment</h3>

        <div class="text-center mb-4">
            <i class="fa-brands fa-cc-visa fa-2x text-muted mx-1"></i>
            <i class="fa-brands fa-cc-mastercard fa-2x text-muted mx-1"></i>
            <i class="fa-brands fa-cc-amex fa-2x text-muted mx-1"></i>
        </div>

        <form action="/confirm-payment" method="POST">
            <input type="hidden" name="email" value="<%= email %>">
            <input type="hidden" name="address" value="<%= address %>">
            <input type="hidden" name="phone" value="<%= phone %>">
            <input type="hidden" name="total" value="<%= total %>">

            <div class="mb-3">
                <label class="form-label fw-bold small text-muted">Cardholder Name</label>
                <input type="text" name="cardName" class="form-control" placeholder="e.g. John Doe" required>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold small text-muted">Card Number</label>
                <div class="input-group">
                    <span class="input-group-text bg-white"><i class="fa-regular fa-credit-card"></i></span>
                    <input type="text" class="form-control" placeholder="0000 0000 0000 0000" maxlength="19" required>
                </div>
            </div>

            <div class="row">
                <div class="col-6 mb-3">
                    <label class="form-label fw-bold small text-muted">Expiration Date</label>
                    <input type="text" class="form-control" placeholder="MM / YY" maxlength="5" required>
                </div>
                <div class="col-6 mb-3">
                    <label class="form-label fw-bold small text-muted">CVV / CVC</label>
                    <input type="password" class="form-control" placeholder="123" maxlength="3" required>
                </div>
            </div>

            <button type="submit" class="btn btn-theme w-100 py-2 fs-5 mt-2">
                Pay Now <i class="fa-solid fa-lock ms-2"></i>
            </button>

            <div class="text-center mt-3">
                <a href="/checkout" class="text-decoration-none text-muted small">
                    &larr; Back to Order Details
                </a>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>